# {{.AppName}} (eletrocromo iOS host)

Ephemeral XcodeGen + WKWebView host. The Go app is a c-archive
(`EletrocromoStart`) that owns loopback HTTP + auth.

Do not commit this tree. Rebuild with:

```
eletrocromo build ios
```
